import React from 'react';
// Styles
import './style.css';

class AppBar extends React.Component {
  render() {
    return (
      <div id="app-bar">
        <span>rounders</span>
      </div>
    )
  }
}

export default AppBar;